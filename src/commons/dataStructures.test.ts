import {describe, test} from 'node:test';
import assert from 'node:assert/strict';

describe('dataStructures', () => {
    test('TODO', async (t) => {
        await t.test(() => assert.ok(true));
    });
});
